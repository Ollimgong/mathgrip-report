# 📊 매쓰그립 수학학원 — 월말평가 레포트 시스템

> **학원명:** 매쓰그립 수학학원  
> **슬로건:** 손에 잡히는 수학  
> **테마 컬러:** 메인 `#19b1c6` (민트) / 서브 `#1e2a6d` (네이비)  
> **현재 버전:** v4  
> **마지막 수정:** 2026-03-30  

---

## 🗂️ 프로젝트 개요

Notion에서 관리하는 월말평가 데이터를 **구글 시트**에 붙여넣기만 하면,  
버튼 하나로 레포트를 출력할 수 있는 **완전 정적 웹앱**.

- 서버 없음 (백엔드 없음)
- 설치 없음 (브라우저에서 바로 실행)
- 인터넷: 구글 시트 연동 시에만 필요
- 데이터 소스: 구글 시트 전용
- **단일 HTML 파일** (CSS + JS 전부 인라인 번들)
- **GitHub Pages**로 호스팅하여 링크 공유 가능

---

## 🌐 배포

**GitHub Pages** 무료 호스팅 사용:
- URL: `https://ollimgong.github.io/mathgrip-report/`
- 저장소에 `index.html` 하나만 올리면 자동 배포
- 업데이트: 저장소에서 파일 교체 → 1~2분 후 반영
- 선생님들은 링크만 즐겨찾기하면 끝

---

## 📁 파일 구조

**배포용:**
```
index.html          ← 이것 하나가 전체 앱 (CSS + JS 인라인 번들)
```

**개발용 (소스 분리):**
```
/
├── index_modules.html      ← 모듈 방식 원본 HTML (빌드 원본)
├── build.py                ← 번들링 스크립트
├── css/
│   └── style.css           ← 전체 스타일
├── js/
│   ├── parser.js           ← MASTER 문자열 파싱 + 통계 계산
│   ├── store.js            ← 앱 상태 관리 (localStorage 포함)
│   ├── renderer.js         ← 레포트 카드 HTML 생성 + 도넛 차트
│   ├── print.js            ← 인쇄 / 미리보기 모달
│   └── googleSheets.js     ← Apps Script 연동 (탭 목록 자동 감지 포함)
└── README.md
```

> **번들링:** `python3 build.py` → `index_bundled.html` 생성 → `index.html`로 교체  
> **외부 라이브러리:** Chart.js 4.4.0 (CDN), Noto Sans KR (Google Fonts)

---

## 🔄 전체 데이터 흐름

```
Notion DB
  └─ MASTER 속성 복사 (파이프 구분 문자열)
       └─ 구글 시트 탭(YYYY-MM)에 붙여넣기
            └─ Apps Script 웹앱 URL로 fetch
                 ├─ 탭 목록 자동 감지 (__list__ 요청)
                 └─ 기준월 + 이전 최대 6개 탭 자동 로드
                      └─ parser.js → parseCSV() → 학생 객체 배열
                           └─ AppStore.setAllStudents()
                                └─ renderAll() → 필터 칩 + 학생 목록 + 레포트 카드
```

---

## 📋 MASTER 필드 명세 (v4 — 15필드)

구글 시트 A열에 한 줄씩 저장되는 파이프(`|`) 구분 문자열.

| 인덱스 | 필드명 | 예시 | 설명 |
|--------|--------|------|------|
| 0 | `id` | `ST-8` | 학생 고유 ID |
| 1 | `month` | `2026년 3월` | 시행월 (한국어 형식 고정) |
| 2 | `name` | `이지현` | 학생 이름 |
| 3 | `teacher` | `박진희 T` | 담당 선생님 |
| 4 | `school` | `한강중학교` | 학교명 |
| 5 | `grade` | `중2` | 학년 (`초1`~`초6`, `중1`~`중3`, `고1`~`고3`) |
| 6 | `score` | `85` | 월말평가 점수 (없으면 빈 문자열) |
| 7 | `scope` | `소인수분해` | 시험 범위 (세미콜론 → ` / ` 치환 표시) |
| 8 | `dateStart` | `260201` | 집계 시작일 (YYMMDD) |
| 9 | `dateEnd` | `260324` | 집계 종료일 (YYMMDD) |
| 10 | `att` | `7,1,0,8` | 출결: `출석,지각,결석,전체` |
| 11 | `att2` | `6,1,0,87` | 수업태도: `★★★,★★☆,★☆☆,평균%` |
| 12 | `hw` | `5,2,0,1,80` | 숙제: `★★★,★★☆,★☆☆,미풀이,평균%` |
| 13 | `progress` | `개념;심화` | 진도 태그 (세미콜론 구분) |
| 14+ | `comment` | `수업 태도 좋음` | 선생님 코멘트 (파이프 포함 가능) |

**Notion MASTER 수식:**
```
prop("학생ID") + "|" + prop("시행월") + "|" + prop("이름") + "|" + prop("담당선생님") + "|" + prop("학교") + "|" + prop("학년") + "|" + (empty(prop("점수")) ? "" : prop("점수")) + "|" + prop("시험범위") + "|" + formatDate(prop("집계시작일"), "YYMMDD") + "|" + formatDate(prop("집계종료일"), "YYMMDD") + "|" + prop("출결집계") + "|" + prop("태도집계") + "|" + prop("숙제집계") + "|" + prop("진도") + "|" + prop("코멘트").replaceAll("\n", " ").trim()
```

---

## 🖥️ UI 구조 (3패널 레이아웃)

```
┌─────────────────────────────────────────────────────────┐
│  헤더: 매쓰그립 월말평가 레포트           🖨️ 선택 인쇄  │
├───────┬──────────┬──────────────────────────────────────┤
│ 레포트│ 학생     │  메인 콘텐츠                          │
│ 설정  │ 목록     │                                      │
│ (좌)  │ (중)     │  ┌ 필터 칩 (선생님 / 학년) ─────────┐ │
│       │          │  │ 전체 | 오미경T | 박진희T | ...   │ │
│ 기준월│ 🔍검색   │  │ 전체 | 초5 | 초6 | 중1 | ...     │ │
│ 선택  │          │  └─────────────────────────────────┘ │
│       │ 정렬     │  ┌ 통계 바 ────────────────────────┐ │
│ 포함월│ ☑ 학생1  │  │ 5명 | 평균 85 | 최고/최저 | 선택 │ │
│ 표시  │ ☑ 학생2  │  └─────────────────────────────────┘ │
│       │ ☐ 학생3  │                                      │
│ 시트  │ ...      │  ┌ 레포트 카드 ─────────────────────┐ │
│ 설정  │          │  │ (선택된 학생만 / 전체)            │ │
└───────┴──────────┴──────────────────────────────────────┘
  220px    260px                나머지
```

**초기 화면:**
- 시트 미연동 → "시트 설정하기" 버튼 (중앙)
- 시트 연동됨 → "🔄 최신 월 불러오기" 버튼 (중앙)
- 로딩 중 → 스피너 + 진행 메시지 (중앙)

---

## ⚙️ Apps Script 코드

구글 시트 → 확장 프로그램 → Apps Script → 아래 코드 붙여넣기 → 새 배포

```javascript
function doGet(e) {
  const sheetId = e.parameter.sheet;
  const tabName = e.parameter.tab;
  const ss = SpreadsheetApp.openById(sheetId);
  if (!tabName || tabName === '__list__') {
    const names = ss.getSheets().map(s => s.getName());
    return ContentService
      .createTextOutput(JSON.stringify({ tabs: names }))
      .setMimeType(ContentService.MimeType.JSON);
  }
  const sheet = ss.getSheetByName(tabName);
  if (!sheet) {
    return ContentService
      .createTextOutput(JSON.stringify({ error: '탭을 찾을 수 없습니다: ' + tabName }))
      .setMimeType(ContentService.MimeType.JSON);
  }
  const data = sheet.getDataRange().getValues();
  const rows = data.map(row => row[0]).filter(v => v && String(v).trim());
  return ContentService
    .createTextOutput(rows.join('\n'))
    .setMimeType(ContentService.MimeType.TEXT);
}
```

**배포 설정:**
- 배포 → 새 배포 → 유형: **웹 앱**
- 다음 사용자로 실행: **나**
- 액세스 권한: **모든 사용자**

---

## 📅 매달 선생님 작업 순서 (3분)

1. Notion 월말평가 DB 열기 → MASTER 열 전체 복사
2. 구글 시트 열기 → 새 탭 추가 `+`
3. 탭 이름을 `2026-04` 형식으로 변경
4. A1 셀에 붙여넣기
5. 앱 열기 → 기준월 선택 → 🔄 불러오기
   - 시트 탭 목록 자동 감지 → 이전 탭 최대 6개 자동 포함
   - 없는 탭은 조용히 건너뜀

---

## 🎨 레포트 카드 구성

1. **헤더** — 로고(46px) + 학원명 + 슬로건 + "2026년 3월 월말평가" 문서 제목
2. **학생 정보** — 이름 · 학교 · 학년 · 담당선생님
3. **이번 달 성적** — 점수(90↑ 녹색, 나머지 기본색) + 시험범위 + 진도
4. **성적 추이** — 좌측 테이블(월/점수/범위) + 우측 막대그래프(0~100 고정, 점수 라벨)
5. **학습 현황** — 출결/수업태도/숙제 도넛 차트 + 집계 기간 표시 (M.D ~ M.D)
6. **선생님 코멘트** — 학년 색상 보더 박스, 최대 400자 10줄 clamp
7. **푸터** — 학원명 · 슬로건

**색상 체계:**
- 막대그래프: 브랜드 민트 통일 (기준월 100%, 이전 달 45%)
- 도넛 차트: 민트(#19b1c6) / 노랑(#f59e0b) / 주황(#f97316) / 빨강(#ef4444)
- 칩: 출석/★★★ = 초록, 지각/★★☆ = 노랑, 결석/★☆☆ = 빨강
- 학년별 색상: 블루/그린/퍼플/오렌지/로즈/틸 (설정에서 커스텀 가능)
- 숙제 미제출: 칩 대신 "전체 N회 (미제출 M회)" 텍스트 표기

**섹션 타이틀:** 이모지 없음, 좌측 민트 액센트 바(3px)

---

## 🔍 필터 시스템

**칩 필터 (메인 영역 상단):**
- 선생님: `전체` | `오미경 T (5)` | `박진희 T (8)` | ... (색상 닷 포함)
- 학년: `전체` | `초5 (3)` | `초6 (7)` | `중1 (5)` | ...
- 다중 선택 가능 (예: 오미경 T + 변선숙 T + 초5 + 초6)
- `전체` 클릭 시 해당 카테고리 초기화
- 각 칩에 인원수 표시

**학생 목록 사이드바:**
- 이름 검색
- 정렬: 이름순(기본) / 점수↑ / 점수↓

**통계 바:** 필터 적용된 그룹 기준 평균/최고/최저 자동 계산

---

## 🖨️ 인쇄 설정

```
@page: A4, margin 8mm 10mm
카드: width 100%, 푸터 하단 고정 (flex + margin-top auto)
폰트: 학원명 15pt, 문서제목 14pt, 본문 10~11pt, 점수 36pt
도넛: 72×72px, 막대: 18px 너비
성적추이 테이블: max-width 70%
```

---

## ✅ 완성된 기능 목록

- [x] 단일 HTML 번들 (file:// 호환, GitHub Pages 호스팅)
- [x] 구글 시트 Apps Script 연동 + 탭 목록 자동 감지
- [x] 3패널 UI (설정 / 학생목록 / 메인)
- [x] 시트 연동 시 중앙 자동 로드 버튼
- [x] 다중 필터 칩 (선생님 + 학년 동시 선택)
- [x] 필터별 그룹 통계
- [x] 학생 정렬 (이름순/점수↑↓)
- [x] 선택된 학생만 카드 미리보기
- [x] A4 인쇄 최적화 (pt 폰트 보정, 푸터 하단 고정)
- [x] 성적 추이: 테이블 + 막대그래프 병렬, 0~100 고정, 점수 라벨
- [x] 집계 기간 표시
- [x] 학년별 색상 (설정에서 커스텀)
- [x] 도넛 차트 + 칩 색상 통일
- [x] 숙제 미제출 텍스트 표기
- [x] 코멘트 400자 10줄 제한
- [x] 점수 색상: 90점↑ 녹색만 강조
- [x] 로고 46px

---

## ⚠️ 알려진 제약사항

1. **Apps Script 재배포** — 코드 수정 후 반드시 새 버전으로 재배포
2. **시트 공개 설정** — 배포 시 액세스를 "모든 사용자"로 설정 필수
3. **MASTER 형식** — 파이프 구분 15필드, 10개 미만이면 건너뜀
4. **month 필드** — `"2026년 3월"` 형식 고정
5. **로고 용량** — localStorage 한도로 500KB 이하 권장
6. **코멘트** — 400자 이내 권장, 초과 시 잘림
